# 🤖 سیستم هوشمند آموزش ترکیبی KURDO-AI

## خلاصه

KURDO-AI حالا یک سیستم هوشمند دارد که **بهترین روش آموزش** را برای شما انتخاب می‌کند!

این سیستم **سه روش مکمل** را پشتیبانی می‌کند:

1. **Fine-Tuning کامل** (OpenAI, HuggingFace, Anthropic)
2. **LoRA** (کارآمد و سریع)
3. **Prompt Engineering** (بدون آموزش، فوری) ⭐ جدید!

---

## 📊 مقایسه روش‌های آموزش

| روش | زمان | هزینه | GPU مورد نیاز | کیفیت | حجم فایل |
|-----|------|--------|--------------|--------|---------|
| **Prompt Engineering** ⭐ | فوری | $0 | هیچ | ⭐⭐⭐ | هیچ |
| **LoRA (8-bit)** | ۰.۵-۲ ساعت | $0 | RTX 3060 12GB | ⭐⭐⭐⭐ | ~50MB |
| **LoRA (4-bit)** | ۱-۳ ساعت | $0 | RTX 2060 6GB | ⭐⭐⭐ | ~50MB |
| **Fine-Tuning کامل** | ۲-۱۰ ساعت | $10-50 | A100 40GB | ⭐⭐⭐⭐⭐ | ~14GB |
| **OpenAI Fine-Tuning** | ۰.۵-۱ ساعت | $5-20 | هیچ (ابری) | ⭐⭐⭐⭐ | ابری |

---

## 🎯 کدام روش را انتخاب کنم؟

### ⭐ **Prompt Engineering** - شروع سریع بدون آموزش! (جدید)

- **مناسب برای:** همه! نیازی به GPU ندارد
- **مزایا:**
  - فوری (بدون آموزش!)
  - رایگان کامل (فقط هزینه inference)
  - بدون نیاز به GPU
  - انعطاف بسیار بالا
  - عالی برای نمونه‌سازی
  - کار با هر مدل/API
- **معایب:**
  - محدودیت context window
  - کمتر ثابت از fine-tuning
  - نیاز به مهارت prompt engineering
- **تکنیک‌ها:**
  - Zero-shot prompting
  - Few-shot learning
  - Chain-of-thought reasoning
  - Prompt templates
  - Cached system prompts

### ✅ **LoRA (8-bit)** - توصیه برای آموزش

- **مناسب برای:** GPU های متوسط (RTX 3060 و بهتر)
- **مزایا:**
  - ۹۰-۹۹٪ پارامترهای کمتر
  - ۳-۱۰ برابر سریع‌تر از Fine-Tuning کامل
  - فایل‌های Adapter کوچک (~50MB)
  - کیفیت تقریباً مشابه Fine-Tuning کامل
- **معایب:**
  - نیاز به GPU دارد
  - کمی پیچیده‌تر از OpenAI

### 💪 **Fine-Tuning کامل (HuggingFace)**

- **مناسب برای:** GPU های قدرتمند (A100, H100)
- **مزایا:**
  - بهترین کیفیت
  - سازگاری کامل با مدل
  - کنترل کامل بر آموزش
- **معایب:**
  - نیاز به GPU قدرتمند (40GB+)
  - کند (ساعت‌ها)
  - فایل‌های بزرگ (~14GB)

### ⚡ **LoRA (4-bit)** - برای GPU های ضعیف

- **مناسب برای:** GPU های کوچک (RTX 2060 6GB+)
- **مزایا:**
  - روی 6GB GPU کار می‌کند!
  - بسیار سریع
  - مصرف حافظه بسیار کم
- **معایب:**
  - کیفیت کمی پایین‌تر از 8-bit
  - از دقت کاسته می‌شود

### ☁️ **OpenAI Fine-Tuning** - بدون GPU

- **مناسب برای:** بدون GPU محلی
- **مزایا:**
  - هیچ GPU نیاز نیست
  - سرویس مدیریت شده (آسان)
  - سریع (۳۰-۶۰ دقیقه)
- **معایب:**
  - هزینه دارد ($5-20)
  - داده به OpenAI فرستاده می‌شود
  - فقط GPT-3.5/4o-mini

### 🔄 **Prompt Caching (Anthropic)** - بدون آموزش

- **مناسب برای:** نمونه‌سازی سریع، بودجه کم
- **مزایا:**
  - بدون آموزش
  - فوری
  - بسیار ارزان
- **معایب:**
  - Fine-Tuning واقعی نیست
  - اثربخشی محدود
  - فقط با Claude کار می‌کند

---

## 🚀 نحوه استفاده

### 1️⃣ دریافت توصیه هوشمند

```python
from cad3d.super_ai.brain import SuperAIBrain

brain = SuperAIBrain()

# سیستم بهترین روش را پیشنهاد می‌دهد
recommendation = brain.recommend_training_method(
    model_size_gb=7.0,           # مدل 7 میلیارد پارامتر
    dataset_size=100,             # 100 نمونه آموزشی
    gpu_memory_gb=12.0,           # RTX 3060 12GB
    provider="local"              # آموزش محلی
)

print(recommendation)
```

**خروجی:**

```json
{
  "recommended_method": "lora_8bit",
  "confidence": 0.95,
  "reasoning": [
    "LoRA uses 90% fewer parameters",
    "8-bit quantization fits in GPU",
    "3-10x faster than full fine-tuning",
    "Quality nearly as good as full FT"
  ],
  "estimated_time_hours": 1.5,
  "estimated_cost_usd": 0,
  "requirements": ["GPU: 6GB+ VRAM (e.g., RTX 3060)"]
}
```

### 2️⃣ آموزش خودکار (سیستم تصمیم می‌گیرد)

```python
# سیستم به طور خودکار بهترین روش را انتخاب و اجرا می‌کند
result = brain.auto_train(
    training_data=my_data,
    adapter_name="kurdo-arch-v1",
    model_name="meta-llama/Llama-2-7b-hf",
    provider="local"
)

print(result)
```

### 3️⃣ مقایسه همه روش‌ها

```python
# مقایسه کامل همه روش‌ها
comparison = brain.compare_all_training_methods(
    model_name="meta-llama/Llama-2-7b-hf",
    dataset_size=100
)

# نمایش جدول مقایسه
for method, details in comparison["methods"].items():
    print(f"\n{method}:")
    print(f"  Time: {details['estimated_time']}")
    print(f"  Cost: {details['estimated_cost']}")
    print(f"  GPU: {details['gpu_required']}")
    print(f"  Pros: {', '.join(details['pros'])}")
```

---

## 📈 سناریوهای واقعی

### سناریو ۱: دانشجوی معماری با RTX 3060 12GB

```python
recommendation = brain.recommend_training_method(
    model_size_gb=7.0,
    dataset_size=50,
    gpu_memory_gb=12.0,
    provider="local"
)
# توصیه: LoRA 8-bit ✅
# زمان: ~1 ساعت
# هزینه: رایگان
```

### سناریو ۲: استارتاپ معماری بدون GPU

```python
recommendation = brain.recommend_training_method(
    model_size_gb=0,
    dataset_size=100,
    budget_usd=20.0,
    provider="openai"
)
# توصیه: OpenAI Fine-Tuning ✅
# زمان: ~30 دقیقه
# هزینه: $10-15
```

### سناریو ۳: محقق با A100 40GB

```python
recommendation = brain.recommend_training_method(
    model_size_gb=7.0,
    dataset_size=500,
    gpu_memory_gb=40.0,
    provider="local"
)
# توصیه: Full Fine-Tuning ✅
# زمان: ~4 ساعت
# هزینه: رایگان (محلی)
```

### سناریو ۴: دانشجوی با RTX 2060 6GB

```python
recommendation = brain.recommend_training_method(
    model_size_gb=7.0,
    dataset_size=30,
    gpu_memory_gb=6.0,
    provider="local"
)
# توصیه: LoRA 4-bit ✅
# زمان: ~2 ساعت
# هزینه: رایگان
```

---

## 🧪 اجرای تست‌ها

### نصب وابستگی‌ها

```bash
pip install peft bitsandbytes accelerate transformers torch
```

### اجرای تست‌های تعاملی

```bash
python cad3d/super_ai/test_hybrid_training.py
```

**منوی تعاملی:**

```
1. 🤖 Intelligent Training Recommendation (4 scenarios)
2. 📊 Comprehensive Methods Comparison Table
3. ⚖️  LoRA vs Full Fine-Tuning (Technical Details)
4. 🧠 KURDO-AI Brain Status
5. 🎯 List Trained LoRA Adapters
6. 📋 List Fine-Tuned Models
7. 🚀 Run All Tests
8. ❌ Exit
```

### اجرای تست خاص

```bash
# فقط سیستم توصیه
python cad3d/super_ai/test_hybrid_training.py --recommend

# جدول مقایسه
python cad3d/super_ai/test_hybrid_training.py --compare

# LoRA در مقابل Fine-Tuning کامل
python cad3d/super_ai/test_hybrid_training.py --lora-vs-ft

# وضعیت Brain
python cad3d/super_ai/test_hybrid_training.py --status

# همه تست‌ها
python cad3d/super_ai/test_hybrid_training.py --all
```

---

## 💡 نکات مهم

### 1. شناسایی خودکار GPU

سیستم به طور خودکار حافظه GPU را تشخیص می‌دهد:

```python
recommendation = brain.recommend_training_method(
    dataset_size=100,
    # gpu_memory_gb خودکار شناسایی می‌شود
    provider="local"
)
```

### 2. محدودیت‌های زمانی و بودجه

```python
recommendation = brain.recommend_training_method(
    dataset_size=100,
    training_time_hours=1.0,  # حداکثر ۱ ساعت
    budget_usd=10.0,          # حداکثر $10
    provider="local"
)
```

### 3. چندین Adapter برای کارهای مختلف

با LoRA می‌توانید چندین Adapter برای کارهای مختلف آموزش دهید:

```python
# Adapter برای نقشه‌های معماری
brain.auto_train(
    training_data=arch_data,
    adapter_name="kurdo-arch-plans"
)

# Adapter برای محاسبات ساختاری
brain.auto_train(
    training_data=struct_data,
    adapter_name="kurdo-structural"
)

# Adapter برای تخمین هزینه
brain.auto_train(
    training_data=cost_data,
    adapter_name="kurdo-cost-estimation"
)

# لیست همه Adapter ها
adapters = brain.list_lora_adapters()
print(adapters)
```

---

## 🔧 تنظیمات پیشرفته

### تنظیمات LoRA

```python
# LoRA با تنظیمات دلخواه
result = brain.train_lora_adapter(
    training_data=data,
    adapter_name="custom-adapter",
    base_model="meta-llama/Llama-2-7b-hf",
    r=32,                    # Rank (4-64)
    lora_alpha=64,           # Alpha (معمولاً 2*r)
    lora_dropout=0.1,        # Dropout (0.0-0.2)
    load_in_8bit=True,       # 8-bit quantization
    num_train_epochs=3,      # تعداد Epoch ها
    learning_rate=2e-4       # نرخ یادگیری
)
```

### تنظیمات Fine-Tuning کامل

```python
# Fine-Tuning با HuggingFace
result = brain.full_fine_tune_workflow(
    provider="huggingface",
    training_data=data,
    base_model="meta-llama/Llama-2-7b-hf",
    num_train_epochs=5,
    learning_rate=2e-5,
    batch_size=4
)
```

---

## 📊 مقایسه تکنیکی دقیق

### مصرف حافظه (مدل 7B)

| روش | حافظه GPU | حافظه RAM | حافظه دیسک |
|-----|-----------|-----------|-------------|
| Full FT | ~28GB | ~16GB | ~14GB |
| LoRA 8-bit | ~8GB | ~8GB | ~50MB |
| LoRA 4-bit | ~4GB | ~4GB | ~50MB |

### پارامترهای قابل آموزش (مدل 7B)

| روش | پارامترهای آموزشی | درصد |
|-----|-------------------|------|
| Full FT | 7,000,000,000 | 100% |
| LoRA (r=16) | ~16,777,216 | 0.24% |
| LoRA (r=8) | ~8,388,608 | 0.12% |

### سرعت آموزش (100 نمونه، GPU A100)

| روش | زمان واقعی | سرعت نسبی |
|-----|-----------|-----------|
| Full FT | ~600 دقیقه | 1x |
| LoRA 8-bit | ~60 دقیقه | 10x ⚡ |
| LoRA 4-bit | ~90 دقیقه | 7x ⚡ |

---

## 🎓 مثال کامل: آموزش مدل معماری

```python
from cad3d.super_ai.brain import SuperAIBrain

# ایجاد Brain
brain = SuperAIBrain()

# داده‌های آموزشی معماری
training_data = [
    {
        "prompt": "محاسبه مساحت یک اتاق با ابعاد 5x4 متر",
        "completion": "مساحت = طول × عرض = 5 × 4 = 20 متر مربع"
    },
    {
        "prompt": "چند آجر برای دیوار 10 متری نیاز است؟",
        "completion": "برای هر متر مربع دیوار تقریباً 60 آجر نیاز است. برای دیوار 10 متری (با ارتفاع 3 متر): 10 × 3 × 60 = 1800 آجر"
    },
    # ... داده‌های بیشتر
]

# گام 1: دریافت توصیه
print("📊 تحلیل بهترین روش آموزش...")
recommendation = brain.recommend_training_method(
    dataset_size=len(training_data),
    provider="local"
)

print(f"✅ روش توصیه شده: {recommendation['recommended_method']}")
print(f"⏱️  زمان تخمینی: {recommendation['estimated_time_hours']:.1f} ساعت")
print(f"💰 هزینه تخمینی: ${recommendation['estimated_cost_usd']:.2f}")
print(f"📝 دلایل: {', '.join(recommendation['reasoning'])}")

# گام 2: آموزش خودکار
print("\n🚀 شروع آموزش خودکار...")
result = brain.auto_train(
    training_data=training_data,
    adapter_name="kurdo-arch-v1",
    model_name="meta-llama/Llama-2-7b-hf"
)

if result.get("status") == "success":
    print("✅ آموزش با موفقیت انجام شد!")
    print(f"📁 Adapter: {result.get('adapter_name')}")
    print(f"📊 متریک‌های آموزش:")
    if "metrics" in result:
        for key, value in result["metrics"].items():
            print(f"  - {key}: {value}")
else:
    print(f"❌ خطا: {result.get('message')}")

# گام 3: لیست همه Adapter ها
print("\n🎯 Adapter های آموزش داده شده:")
adapters = brain.list_lora_adapters()
for adapter in adapters.get("adapters", []):
    print(f"  - {adapter}")

print("\n✅ تمام!")
```

---

## 🆘 رفع مشکلات

### خطا: "LoRA module not available"

```bash
pip install peft bitsandbytes accelerate
```

### خطا: "CUDA out of memory"

دو راه حل:

1. استفاده از LoRA 4-bit:

```python
recommendation = brain.recommend_training_method(
    gpu_memory_gb=6.0,  # GPU کوچک
    provider="local"
)
```

2. یا استفاده از OpenAI:

```python
recommendation = brain.recommend_training_method(
    budget_usd=20.0,
    provider="openai"
)
```

### خطا: "No GPU available"

استفاده از Fine-Tuning ابری:

```python
# OpenAI
result = brain.auto_train(
    training_data=data,
    provider="openai"
)

# یا Anthropic Prompt Caching
result = brain.auto_train(
    training_data=data,
    provider="anthropic"
)
```

---

## 📚 منابع بیشتر

### کدهای مرتبط

- `cad3d/super_ai/fine_tuning.py` - Fine-Tuning کامل
- `cad3d/super_ai/lora_training.py` - LoRA
- `cad3d/super_ai/hybrid_training.py` - سیستم هوشمند
- `cad3d/super_ai/brain.py` - Brain اصلی

### مستندات

- LoRA: <https://arxiv.org/abs/2106.09685>
- PEFT: <https://github.com/huggingface/peft>
- Transformers: <https://huggingface.co/docs/transformers>

---

## 🎉 خلاصه

KURDO-AI حالا یک سیستم **هوشمند و خودکار** برای آموزش مدل دارد!

**ویژگی‌های کلیدی:**

- ✅ **انتخاب خودکار** بهترین روش آموزش
- ✅ **شناسایی خودکار** قابلیت‌های GPU
- ✅ **مقایسه جامع** همه روش‌ها
- ✅ **پشتیبانی از 5 روش** آموزش مختلف
- ✅ **بهینه‌سازی هزینه و زمان**

**برای شروع:**

```python
from cad3d.super_ai.brain import SuperAIBrain
brain = SuperAIBrain()

# دریافت توصیه
rec = brain.recommend_training_method(dataset_size=100)

# آموزش خودکار
result = brain.auto_train(training_data=my_data)
```

**موفق باشید! 🚀**
