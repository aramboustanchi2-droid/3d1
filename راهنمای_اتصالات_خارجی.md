# راهنمای فعال‌سازی کامل اتصالات خارجی KURDO-AI

## دستورالعمل گام‌به‌گام برای اتصال دائمی به پایگاه‌های هوش مصنوعی جهانی

---

## 🎯 هدف

این راهنما به شما کمک می‌کند تا سیستم KURDO-AI را از یک مدل محلی به یک **سیستم متصل به اینترنت با قابلیت یادگیری پیوسته از منابع جهانی** تبدیل کنید.

پس از اجرای این مراحل، سیستم به طور خودکار:

- از Google Search برای تحقیق آنلاین استفاده می‌کند
- از Google Translate برای ترجمه واقعی بهره می‌برد
- از ChatGPT، Claude، Grok و DeepSeek برای تولید پاسخ‌های پیشرفته کمک می‌گیرد
- به طور مداوم از تمام این منابع داده جمع‌آوری و آموزش می‌بیند

---

## 📋 پیش‌نیازها

### 1. نصب کتابخانه‌های جدید

```powershell
pip install requests beautifulsoup4 python-dotenv
```

### 2. دریافت کلیدهای API

#### 🔵 Google (Search + Translate)

1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. یک پروژه جدید بسازید یا پروژه موجود را انتخاب کنید
3. **API & Services** → **Enable APIs and Services**
4. جستجو و فعال کنید:
   - **Custom Search API**
   - **Cloud Translation API**
5. **Credentials** → **Create Credentials** → **API Key**
6. کلید را کپی کنید

**برای Google Search:**
7. به [Programmable Search Engine](https://programmablesearchengine.google.com/) بروید
8. یک موتور جستجوی سفارشی بسازید
9. **Search the entire web** را فعال کنید
10. Search Engine ID را کپی کنید

#### 🟢 OpenAI (ChatGPT)

1. به [OpenAI Platform](https://platform.openai.com/) بروید
2. ثبت‌نام یا ورود کنید
3. **API Keys** → **Create new secret key**
4. کلید را کپی و در جای امن ذخیره کنید
5. **افزایش اعتبار:** برای استفاده مداوم، یک روش پرداخت اضافه کنید

#### 🟣 Anthropic (Claude)

1. به [Anthropic Console](https://console.anthropic.com/) بروید
2. ثبت‌نام کنید (ممکن است نیاز به درخواست دسترسی داشته باشید)
3. **Settings** → **API Keys** → **Create Key**
4. کلید را کپی کنید

#### 🔴 Grok (X AI)

⚠️ **توجه:** API عمومی Grok هنوز منتشر نشده است. این بخش آماده است ولی فعلاً غیرفعال.

#### 🟠 DeepSeek

1. به [DeepSeek Platform](https://platform.deepseek.com/) بروید
2. ثبت‌نام کنید
3. API Key خود را دریافت کنید

---

## ⚙️ پیکربندی

### مرحله 1: تنظیم فایل .env

1. فایل `.env.example` را در مسیر اصلی پروژه پیدا کنید
2. آن را به `.env` تغییر نام دهید
3. کلیدهای API خود را جایگزین کنید:

```env
# Google
GOOGLE_API_KEY="YOUR_ACTUAL_GOOGLE_API_KEY_HERE"
GOOGLE_CSE_ID="YOUR_CUSTOM_SEARCH_ENGINE_ID_HERE"

# OpenAI
OPENAI_API_KEY="sk-proj-xxxxxxxxxxxxxxxxxxxxx"

# Anthropic
ANTHROPIC_API_KEY="sk-ant-xxxxxxxxxxxxxxxxxxxxx"

# DeepSeek (اختیاری)
DEEPSEEK_API_KEY="your_deepseek_key_here"
```

### مرحله 2: تنظیم سرویس‌های فعال

فایل `cad3d/super_ai/connectors_config.json` را ویرایش کنید:

```json
{
  "google_search": {
    "enabled": true
  },
  "google_translate": {
    "enabled": true
  },
  "openai": {
    "enabled": true,
    "model": "gpt-4o"
  },
  "anthropic": {
    "enabled": true,
    "model": "claude-3-opus-20240229"
  }
}
```

**نکته:** هر سرویسی که کلید API ندارید، `"enabled": false` قرار دهید.

---

## 🚀 راه‌اندازی و تست

### مرحله 1: اجرای داشبورد

```powershell
streamlit run cad3d/dashboard/app.py
```

### مرحله 2: تست اتصالات

در تب **💬 Public Chat** داشبورد، این سوالات را بپرسید:

#### تست ترجمه واقعی

```
طراحی یک ساختمان اداری مدرن
```

سیستم باید:

1. تشخیص دهد متن فارسی است
2. با Google Translate به انگلیسی ترجمه کند
3. پردازش کند
4. پاسخ را به فارسی برگرداند (بدون برچسب‌های [فارسی دست‌وپا شکسته])

#### تست تحقیق آنلاین

```
آخرین روندهای معماری پایدار در سال 2025
```

سیستم باید:

1. در Google جستجو کند
2. نتایج را به پاسخ اضافه کند
3. با ChatGPT یا Claude پاسخ جامع تولید کند

---

## 📊 نحوه کار سیستم (معماری داخلی)

```
درخواست کاربر
    ↓
[تشخیص زبان]
    ↓
[ترجمه به انگلیسی - Google Translate]
    ↓
[تحقیق آنلاین - Google Search] ← (3 نتیجه برتر)
    ↓
[پردازش شوراها - 7 مرحله]
    ↓
[تولید پاسخ پیشرفته - ChatGPT/Claude]
    ↓
[ترجمه برگشت - Google Translate]
    ↓
نمایش به کاربر
```

---

## 🎛️ کنترل و مدیریت

### فعال/غیرفعال کردن سرویس‌ها

در هر زمان می‌توانید سرویس‌ها را در `connectors_config.json` کنترل کنید:

```json
{
  "openai": {
    "enabled": false  // ChatGPT غیرفعال شد
  },
  "anthropic": {
    "enabled": true   // Claude فعال است
  }
}
```

### مانیتورینگ هزینه‌ها

**OpenAI:**

- داشبورد: <https://platform.openai.com/usage>
- هزینه تقریبی: $0.01 per 1K tokens (gpt-4o)

**Anthropic:**

- داشبورد: <https://console.anthropic.com/settings/cost>
- هزینه تقریبی: $0.015 per 1K tokens (Claude Opus)

**Google:**

- داشبورد: <https://console.cloud.google.com/billing>
- Custom Search: 100 جستجوی رایگان/روز، سپس $5 per 1000 queries
- Translation: $20 per 1M characters

---

## 🔒 امنیت

### محافظت از کلیدهای API

1. **هرگز** فایل `.env` را commit نکنید
2. در `.gitignore` اضافه کنید:

```
.env
*.env
```

3. برای تیم، فقط `.env.example` را به اشتراک بگذارید

### تست بدون کلیدهای واقعی

اگر کلید ندارید، سیستم به صورت خودکار به حالت Fallback می‌رود:

- ترجمه: دیکشنری داخلی
- جستجو: غیرفعال
- پاسخ AI: پاسخ‌های ساختاریافته شوراها

---

## 🐛 عیب‌یابی

### خطا: "API Key not configured"

**حل:** مطمئن شوید فایل `.env` در مسیر اصلی پروژه قرار دارد و کلیدها صحیح هستند.

### خطا: "Quota exceeded"

**حل:** سهمیه API تمام شده. به داشبورد مربوطه بروید و اعتبار اضافه کنید.

### خطا: "Import Error: external_connectors"

**حل:** مطمئن شوید `cad3d/super_ai/external_connectors.py` وجود دارد.

### پاسخ‌ها هنوز "الکی" هستند

**بررسی:**

1. لاگ‌ها را بررسی کنید: آیا پیام "Successfully translated" می‌آید؟
2. در `connectors_config.json` بررسی کنید که `enabled: true` است
3. تست کنید: `python -c "from cad3d.super_ai.external_connectors import unified_connector; print(unified_connector.is_enabled('google_translate'))"`

---

## 📚 منابع بیشتر

- [Google Cloud Translation API](https://cloud.google.com/translate/docs)
- [OpenAI API Docs](https://platform.openai.com/docs/api-reference)
- [Anthropic Claude API](https://docs.anthropic.com/claude/reference/getting-started-with-the-api)
- [Google Custom Search JSON API](https://developers.google.com/custom-search/v1/overview)

---

## 🎉 تبریک

سیستم KURDO-AI شما اکنون به یک **ابرسیستم متصل به جهان** تبدیل شده است که:

- ✅ به طور مداوم از منابع جهانی یاد می‌گیرد
- ✅ با کیفیت حرفه‌ای ترجمه می‌کند
- ✅ پاسخ‌های پیشرفته و دقیق تولید می‌کند
- ✅ تحقیقات آنلاین را در پاسخ‌ها ادغام می‌کند

**نکته نهایی:** این اتصالات تا زمانی که شما آن‌ها را در `connectors_config.json` غیرفعال نکنید، فعال خواهند ماند و سیستم به طور خودکار از آن‌ها استفاده خواهد کرد.

برای جداسازی کامل، تمام سرویس‌ها را `"enabled": false` کنید یا فایل `.env` را حذف نمایید.
