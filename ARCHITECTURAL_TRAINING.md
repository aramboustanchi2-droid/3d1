# آموزش و قابلیت‌های تحلیل معماری

# Architectural Analysis Training & Capabilities

## قابلیت‌های آموزش داده شده / Trained Capabilities

سیستم تحلیل معماری این هوش مصنوعی می‌تواند:

### 1. شناسایی انواع نقشه / Drawing Type Detection

- ✅ **پلان (Plan)**: نقشه‌های افقی طبقات
- ✅ **نما (Elevation)**: نماهای عمودی ساختمان
- ✅ **برش (Section)**: برش‌های عمودی
- ✅ **پلان سایت (Site Plan)**: موقعیت کلی ساختمان
- ✅ **جزئیات (Detail)**: جزئیات ساختمانی

### 2. شناسایی فضاها / Space Recognition

سیستم می‌تواند 12 نوع فضا را شناسایی کند:

#### فضاهای مسکونی

- 🛏️ **اتاق خواب (Bedroom)**: با کلمات کلیدی: bedroom, اتاق خواب, master
- 🛋️ **پذیرایی (Living Room)**: living, پذیرایی, نشیمن, هال, سالن
- 🍳 **آشپزخانه (Kitchen)**: kitchen, آشپزخانه, کیچن
- 🚿 **سرویس بهداشتی (Bathroom)**: bathroom, حمام, توالت, WC
- 🚪 **راهرو (Corridor)**: corridor, راهرو, ورودی
- 🌿 **بالکن (Balcony)**: balcony, تراس, بالکن

#### فضاهای خدماتی

- 📦 **انباری (Storage)**: storage, انبار, انباری
- 🚗 **پارکینگ (Parking)**: parking, پارکینگ, گاراژ
- 🪜 **راه پله (Staircase)**: stair, راه پله, پله
- 🛗 **آسانسور (Elevator)**: elevator, آسانسور, lift
- 🏢 **لابی (Lobby)**: lobby, لابی, ورودی
- 💼 **دفتر کار (Office)**: office, دفتر

### 3. تحلیل ابعاد و اندازه‌ها / Dimensional Analysis

برای هر فضا محاسبه می‌شود:

- 📏 **مساحت (Area)**: به متر مربع
- 📐 **محیط (Perimeter)**: طول کل دیوارها
- ↔️ **عرض (Width)**: عرض تقریبی
- ↕️ **طول (Length)**: طول تقریبی
- 📍 **مرکز (Center)**: مختصات مرکز فضا

### 4. تشخیص دیوارها / Wall Detection

- 🧱 تشخیص خطوط دیوار از لایه‌های: WALLS, WALL, دیوار
- 📏 محاسبه طول هر دیوار
- 🏗️ تشخیص دیوارهای خارجی و داخلی

### 5. استخراج ابعاد / Dimension Extraction

- 📐 خواندن ابعاد از entity های DIMENSION
- 🔢 استخراج مقادیر عددی
- 📝 ذخیره متن و مقدار

### 6. آمار و گزارش‌دهی / Statistics & Reporting

#### گزارش‌های تولیدی

- 📄 **گزارش متنی (Text Report)**: گزارش کامل فارسی/انگلیسی
- 📊 **JSON Export**: داده‌های ساختاریافته برای تحلیل
- 📈 **CSV Export**: جدول اتاق‌ها و مشخصات
- 📉 **Statistics**: آمار کلی dataset

## نحوه استفاده / Usage

### تحلیل یک نقشه / Analyze Single Drawing

```powershell
python -m cad3d.cli analyze-architectural --input floor_plan.dxf --output-dir analysis --report --export-json --export-csv
```

### ساخت Dataset از پوشه / Build Dataset from Folder

```powershell
python -m cad3d.cli analyze-architectural --input samples/ --output-dir dataset --recursive --export-json --export-csv
```

## آموزش از نقشه‌های جدید / Learning from New Drawings

سیستم از سه روش برای شناسایی فضاها استفاده می‌کند:

### 1. تشخیص بر اساس متن / Text-Based Recognition

```python
# کلمات کلیدی فارسی و انگلیسی
SPACE_KEYWORDS = {
    SpaceType.BEDROOM: ["bedroom", "bed room", "اتاق خواب", "خواب", "master"],
    SpaceType.KITCHEN: ["kitchen", "آشپزخانه", "اشپزخانه", "کیچن"],
    ...
}
```

### 2. تشخیص بر اساس مساحت / Area-Based Recognition

```python
# حدس هوشمند بر اساس اندازه
if area_m2 < 3:
    return SpaceType.BATHROOM  # کوچک = حمام
elif area_m2 < 8:
    return SpaceType.STORAGE   # متوسط کوچک = انباری
elif area_m2 < 15:
    return SpaceType.BEDROOM   # متوسط = اتاق خواب
elif area_m2 < 30:
    return SpaceType.LIVING_ROOM  # بزرگ = پذیرایی
```

### 3. تشخیص بر اساس لایه / Layer-Based Recognition

```python
# لایه‌های استاندارد CAD
WALL_LAYERS = ["WALLS", "WALL", "دیوار", "A-WALL"]
ROOM_LAYERS = ["ROOMS", "SPACES", "اتاق", "A-AREA"]
TEXT_LAYERS = ["TEXT", "NOTES", "متن", "A-ANNO-TEXT"]
```

## فرمت خروجی / Output Format

### JSON Structure

```json
{
  "drawing_type": "plan",
  "total_area": 73.84,
  "num_rooms": 3,
  "rooms": [
    {
      "name": "Living Room",
      "type": "living_room",
      "area": 29.64,
      "width": 3.8,
      "length": 7.8
    }
  ]
}
```

### CSV Format

```csv
drawing_file,room_name,space_type,area_m2,width_m,length_m,perimeter_m,layer,text_entities
plan.dxf,Living Room,living_room,29.64,3.80,7.80,23.20,ROOMS,Living Room; پذیرایی
```

## ساخت Dataset برای آموزش / Building Training Dataset

برای آموزش سیستم از هزاران نقشه:

```python
from cad3d.dataset_builder import ArchitecturalDatasetBuilder

# ساخت dataset builder
builder = ArchitecturalDatasetBuilder("output/dataset")

# پردازش هزاران نقشه
builder.process_folder("path/to/thousands/of/plans", recursive=True)

# خروجی dataset
builder.export_to_json()  # JSON کامل
builder.export_rooms_to_csv()  # CSV اتاق‌ها
builder.export_statistics()  # آمار

# گزارش خلاصه
summary = builder.generate_summary_report()
print(summary)
```

## آمار Dataset نمونه / Sample Dataset Statistics

```
📊 تعداد کل نقشه‌ها: 10
🏠 تعداد کل فضاها: 8
🧱 تعداد کل دیوارها: 6
📐 مساحت کل: 41.00 متر مربع

میانگین‌ها:
  • فضا در هر نقشه: 0.8
  • دیوار در هر نقشه: 0.6
  • مساحت هر نقشه: 4.10 m²

انواع فضا:
  • bathroom: 4 (50.0%)
  • living_room: 2 (25.0%)
  • storage: 2 (25.0%)
```

## توسعه آینده / Future Development

### برای آموزش بهتر

1. **افزودن نمونه‌های بیشتر**: پردازش هزاران پلان واقعی
2. **کلمات کلیدی بیشتر**: افزودن اصطلاحات محلی و استانداردها
3. **یادگیری ماشین**: استفاده از ML برای بهبود دقت
4. **تشخیص الگو**: شناسایی مبلمان و تجهیزات

### قابلیت‌های پیشنهادی

- 🪑 تشخیص مبلمان (تخت، میز، صندلی)
- 🚪 شناسایی درها و پنجره‌ها
- 🏗️ تحلیل سازه (دیوارهای باربر)
- 🔥 مسیرهای خروج اضطراری
- ♿ بررسی دسترسی معلولین
- 💡 بررسی روشنایی و تهویه

## یادآوری مهم / Important Note

این سیستم یک **پایه هوشمند** برای درک نقشه‌های معماری است که با آموزش بیشتر و افزودن نمونه‌های متنوع‌تر، دقت آن به طور قابل توجهی بهبود می‌یابد.

برای آموزش کامل، نیاز است:

- 📚 1000+ نقشه پلان مختلف
- 🏢 200+ نقشه نما
- ✂️ 200+ نقشه برش
- 🏗️ 100+ پلان سایت

با این حجم داده، سیستم می‌تواند به درک عمیق از معماری دست یابد!
