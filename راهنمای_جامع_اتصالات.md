# 🚀 راهنمای جامع اتصال KURDO-AI به پلتفرم‌های جهانی هوش مصنوعی

## راهنمای کامل فعال‌سازی و استفاده از اتصالات خارجی

---

## 📋 معرفی سیستم چند‌پلتفرمی

سیستم KURDO-AI اکنون به **بیش از ۱۵ پلتفرم پیشرو هوش مصنوعی در جهان** متصل است و به صورت **سقوط پلکانی خودکار (Cascading Fallback)** عمل می‌کند.

### 🎯 مفهوم Cascading Fallback (سقوط پلکانی خودکار)

اگر سرویس اول در دسترس نباشد یا خطا بدهد، سیستم به طور کاملاً خودکار از سرویس بعدی استفاده می‌کند:

**مثال ترجمه:**

```
کاربر: "Translate 'Hello' to Persian"
سیستم:
  ├─ 1️⃣ تلاش DeepL → ❌ خطا (کلید API موجود نیست)
  ├─ 2️⃣ تلاش Microsoft Translator → ❌ خطا
  ├─ 3️⃣ تلاش Google Translate → ✅ موفق: "سلام"
  └─ نتیجه: "سلام"
```

**مثال گفتگو:**

```
کاربر: "امکان‌سنجی ساختمان"
سیستم:
  ├─ 1️⃣ تلاش Claude-3 → ✅ موفق
  └─ پاسخ دقیق و حرفه‌ای از Claude
```

---

## 🌍 سرویس‌های پشتیبانی‌شده

### 🔤 سرویس‌های ترجمه (۶ سرویس)

| اولویت | سرویس | ویژگی | نیاز به API |
|--------|-------|--------|-------------|
| 🥇 | **DeepL** | بالاترین کیفیت ترجمه | ✅ بله |
| 🥈 | Microsoft Translator | سریع و دقیق | ✅ بله |
| 🥉 | Google Translate | پوشش زبانی گسترده | ✅ بله |
| 4️⃣ | LibreTranslate | رایگان/خودمیزبان | ⚠️ اختیاری |
| 5️⃣ | Amazon Translate | AWS | ✅ بله |
| 6️⃣ | **Argos Translate** | **کاملاً آفلاین** | ❌ خیر |

### 💬 سرویس‌های گفتگو و LLM (۶+ سرویس)

| اولویت | سرویس | مدل | نیاز به API |
|--------|-------|-----|-------------|
| 🥇 | **Anthropic Claude** | Claude-3 Opus/Sonnet | ✅ بله |
| 🥈 | OpenAI | GPT-4o, GPT-4 | ✅ بله |
| 🥉 | Google AI Studio | Gemini Pro/Ultra | ✅ بله |
| 4️⃣ | DeepSeek | DeepSeek-V2 | ✅ بله |
| 5️⃣ | Grok | Grok-1 | ✅ بله |
| 6️⃣ | HuggingFace | ۱۰۰,۰۰۰+ مدل | ✅ بله |
| 7️⃣ | **مدل‌های محلی** | Flan-T5, mT5, Gemma | ❌ خیر |

### 🔍 سرویس جستجو

- **Google Custom Search** - تحقیق آنلاین و غنی‌سازی زمینه

---

## 🔧 راهنمای نصب و راه‌اندازی

### مرحله ۱: نصب کتابخانه‌های پایه

```bash
# نیازمندی‌های اصلی (اجباری)
pip install requests beautifulsoup4 python-dotenv

# مدل‌های محلی (اختیاری - برای عملکرد آفلاین)
pip install transformers torch sentencepiece argostranslate
```

### مرحله ۲: ایجاد فایل تنظیمات

```bash
# در لینوکس/مک:
cp .env.example .env

# در ویندوز (PowerShell):
Copy-Item .env.example .env

# یا به صورت دستی:
# فایل .env.example را کپی کرده و به .env تغییر نام دهید
```

### مرحله ۳: دریافت کلیدهای API

در ادامه برای هر سرویس توضیح می‌دهیم.

---

## 🔑 راهنمای گام‌به‌گام دریافت کلیدها

### 1️⃣ Google Translate API

**مراحل:**

1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. پروژه جدید بسازید یا پروژه موجود را انتخاب کنید
3. از منوی چپ: **APIs & Services** → **Library**
4. جستجو کنید: `Cloud Translation API`
5. روی **Enable** کلیک کنید
6. به **Credentials** بروید → **Create Credentials** → **API Key**
7. کلید را کپی کنید

**در فایل .env اضافه کنید:**

```bash
GOOGLE_TRANSLATE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXX
```

---

### 2️⃣ Google AI Studio (Gemini)

**مراحل:**

1. به [Google AI Studio](https://makersuite.google.com/app/apikey) بروید
2. با اکانت Google وارد شوید
3. روی **Get API Key** کلیک کنید
4. **Create API Key** را انتخاب کنید

**در فایل .env اضافه کنید:**

```bash
GOOGLE_AI_STUDIO_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXX
```

---

### 3️⃣ Google Custom Search (برای تحقیق آنلاین)

**بخش ۱: دریافت API Key**

1. همان کلید Google Cloud را می‌توانید استفاده کنید
2. یا API جدید از [Console](https://console.cloud.google.com/) بسازید
3. `Custom Search API` را فعال کنید

**بخش ۲: ایجاد Search Engine**

1. به [Programmable Search Engine](https://programmablesearchengine.google.com/) بروید
2. **Add** را کلیک کنید
3. نام دلخواه وارد کنید
4. **Search the entire web** را فعال کنید
5. روی **Create** کلیک کنید
6. **Control Panel** → کد `cx` را کپی کنید

**در فایل .env اضافه کنید:**

```bash
GOOGLE_SEARCH_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXX
GOOGLE_SEARCH_CX=0123456789abcdef:xyz
```

---

### 4️⃣ Microsoft Translator (Azure)

**مراحل:**

1. به [Azure Portal](https://portal.azure.com/) بروید
2. **Create a resource** → جستجوی `Translator`
3. **Create** را کلیک کنید
4. Region را انتخاب کنید (مثلاً `West US 2`)
5. Pricing tier را انتخاب کنید (F0 برای رایگان)
6. پس از ایجاد به منبع بروید
7. از منوی چپ **Keys and Endpoint** را انتخاب کنید
8. **Key 1** و **Location/Region** را کپی کنید

**در فایل .env اضافه کنید:**

```bash
AZURE_TRANSLATOR_KEY=abc123def456...
AZURE_TRANSLATOR_REGION=westus2
```

---

### 5️⃣ OpenAI (ChatGPT)

**مراحل:**

1. به [OpenAI Platform](https://platform.openai.com/) بروید
2. ثبت‌نام یا ورود کنید
3. از منوی بالا **API Keys** را انتخاب کنید
4. **Create new secret key** را کلیک کنید
5. نام دلخواه بدهید و ایجاد کنید
6. کلید را کپی کنید (فقط یک بار نمایش داده می‌شود!)

**در فایل .env اضافه کنید:**

```bash
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

---

### 6️⃣ Anthropic (Claude)

**مراحل:**

1. به [Anthropic Console](https://console.anthropic.com/) بروید
2. ثبت‌نام یا ورود کنید
3. **API Keys** → **Create Key**
4. نام بدهید و کلید را کپی کنید

**در فایل .env اضافه کنید:**

```bash
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

---

### 7️⃣ DeepL (ترجمه با کیفیت بالا)

**مراحل:**

1. به [DeepL API](https://www.deepl.com/pro-api) بروید
2. ثبت‌نام کنید (پلن Free تا ۵۰۰,۰۰۰ کاراکتر/ماه رایگان)
3. پس از ورود به **Account** → **API Keys** بروید
4. کلید را کپی کنید

**در فایل .env اضافه کنید:**

```bash
DEEPL_API_KEY=abc12345-xxxx-yyyy-zzzz-123456789abc:fx
```

---

### 8️⃣ Grok (X AI)

**مراحل:**

1. به [X AI Console](https://console.x.ai/) بروید
2. با اکانت X (Twitter) وارد شوید
3. API Key بسازید

**در فایل .env اضافه کنید:**

```bash
GROK_API_KEY=xai-XXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

---

### 9️⃣ DeepSeek

**مراحل:**

1. به [DeepSeek Platform](https://platform.deepseek.com/) بروید
2. ثبت‌نام کنید
3. API Key بسازید

**در فایل .env اضافه کنید:**

```bash
DEEPSEEK_API_KEY=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

---

### 🔟 Amazon Translate (AWS)

**مراحل:**

1. به [AWS Console](https://console.aws.amazon.com/) بروید
2. **IAM** → **Users** → **Add User**
3. Access type: **Programmatic access**
4. Attach policy: **TranslateFullAccess**
5. **Access Key ID** و **Secret Access Key** را کپی کنید

**در فایل .env اضافه کنید:**

```bash
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

### 1️⃣1️⃣ LibreTranslate (رایگان/خودمیزبان)

**گزینه ۱: استفاده از سرویس عمومی (رایگان)**

```bash
LIBRETRANSLATE_API_URL=https://libretranslate.com/translate
# API Key اختیاری است
```

**گزینه ۲: خودمیزبانی**

```bash
# نصب:
pip install libretranslate
# اجرا:
libretranslate --host 0.0.0.0

# در .env:
LIBRETRANSLATE_API_URL=http://localhost:5000/translate
```

---

### 1️⃣2️⃣ HuggingFace (دسترسی به ۱۰۰,۰۰۰+ مدل)

**مراحل:**

1. به [HuggingFace](https://huggingface.co/) بروید
2. ثبت‌نام یا ورود کنید
3. **Settings** → **Access Tokens**
4. **New Token** → **Read** را انتخاب کنید

**در فایل .env اضافه کنید:**

```bash
HUGGINGFACE_API_KEY=hf_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

---

### 1️⃣3️⃣ مدل‌های محلی (Offline - بدون نیاز به کلید)

**نصب Argos Translate (ترجمه آفلاین):**

```bash
pip install argostranslate

# دانلود بسته زبانی (انگلیسی-فارسی):
python -c "
import argostranslate.package
argostranslate.package.update_package_index()
available = argostranslate.package.get_available_packages()
pkg = next(p for p in available if p.from_code=='en' and p.to_code=='fa')
argostranslate.package.install_from_path(pkg.download())
"
```

**نصب Transformers (برای Flan-T5, mT5, etc.):**

```bash
pip install transformers torch

# اولین استفاده مدل را دانلود می‌کند (~800MB):
python -c "from transformers import AutoModelForSeq2SeqLM; AutoModelForSeq2SeqLM.from_pretrained('google/flan-t5-base')"
```

---

## ⚙️ فعال‌سازی سرویس‌ها

### ویرایش فایل پیکربندی

فایل `cad3d/super_ai/connectors_config.json` را باز کنید:

```json
{
  "google_translate": {
    "enabled": true,  // ✅ تغییر به true برای فعال‌سازی
    "api_key_env": "GOOGLE_TRANSLATE_API_KEY"
  },
  "openai": {
    "enabled": true,  // ✅ فعال
    "api_key_env": "OPENAI_API_KEY",
    "model": "gpt-4o"
  },
  "anthropic": {
    "enabled": true,  // ✅ فعال
    "api_key_env": "ANTHROPIC_API_KEY",
    "model": "claude-3-opus-20240229"
  },
  "deepl": {
    "enabled": true,  // ✅ فعال برای بهترین کیفیت ترجمه
    "api_key_env": "DEEPL_API_KEY"
  }
  // ... بقیه سرویس‌ها
}
```

**⚠️ نکته مهم:** فقط سرویس‌هایی را فعال کنید که کلید API معتبر دارید!

---

## 🚀 استفاده از سیستم

### تست ساده

ایجاد فایل `test_connectors.py`:

```python
from cad3d.super_ai.external_connectors import unified_connector

# تست ترجمه
print("=== تست ترجمه ===")
result = unified_connector.translate("Hello world", "fa")
print(f"نتیجه: {result}")

# تست گفتگو
print("\n=== تست گفتگو ===")
response = unified_connector.chat_with_fallback(
    prompt="توضیح کوتاهی درباره امکان‌سنجی معماری بده",
    system_prompt="تو یک مشاور معماری حرفه‌ای هستی."
)
print(f"پاسخ: {response.get('content', [{}])[0].get('text', 'خطا')}")

# تست جستجو
if unified_connector.is_enabled("google_search"):
    print("\n=== تست جستجو ===")
    results = unified_connector.search("معماری مدرن", num_results=3)
    print(f"تعداد نتایج: {len(results.get('items', []))}")
```

**اجرا:**

```bash
python test_connectors.py
```

---

### استفاده در KURDO-AI

```python
from cad3d.super_ai.brain import SuperAIBrain

brain = SuperAIBrain()

# پردازش درخواست به فارسی
result = brain.process_request(
    "امکان‌سنجی یک برج ۲۰ طبقه در زمین ۱۰۰۰ متری",
    context_data={
        "site_area": 1000,
        "dimensions": {"length": 40, "width": 25},
        "proposed_height": 60
    }
)

print(result)
```

**سیستم به طور خودکار:**

1. ✅ زبان فارسی را تشخیص می‌دهد
2. ✅ با DeepL/Google ترجمه می‌کند
3. ✅ تحقیق آنلاین انجام می‌دهد (Google Search)
4. ✅ تحلیل امکان‌سنجی می‌کند
5. ✅ پاسخ را با Claude/GPT غنی می‌کند
6. ✅ به فارسی برمی‌گرداند

---

## 📊 نظارت و عیب‌یابی

### بررسی وضعیت سرویس‌ها

```python
from cad3d.super_ai.external_connectors import unified_connector

services = [
    "google_translate", "deepl", "microsoft_translator",
    "openai", "anthropic", "google_ai_studio",
    "grok", "deepseek", "huggingface"
]

print("وضعیت سرویس‌ها:")
print("=" * 50)
for service in services:
    status = "✅ فعال" if unified_connector.is_enabled(service) else "❌ غیرفعال"
    print(f"{service:25} {status}")
```

### لاگ‌های سیستم

سیستم به طور خودکار وضعیت را چاپ می‌کند:

```
[TRANSLATION] Attempting deepl...
[TRANSLATION] deepl failed: API key not found
[TRANSLATION] Attempting microsoft_translator...
[TRANSLATION] Success with microsoft_translator
```

```
[CHAT] Attempting anthropic...
[CHAT] Success with anthropic
```

---

## 💡 بهترین روش‌ها

### 🎯 استراتژی کاهش هزینه

1. **شروع با سرویس‌های رایگان:**
   - LibreTranslate (ترجمه رایگان)
   - HuggingFace (محدودیت سخاوتمندانه)
   - Argos Translate (کاملاً آفلاین)

2. **استفاده از Local Models:**

   ```bash
   pip install transformers argostranslate
   ```

3. **فعال‌سازی انتخابی:**
   - فقط سرویس‌های مورد نیاز را enable کنید

4. **نظارت مداوم:**
   - داشبورد هر سرویس را برای بررسی مصرف چک کنید

### 🏆 بهینه‌سازی کیفیت

1. **برای ترجمه:**
   - اولویت اول: **DeepL** (بالاترین کیفیت)
   - پشتیبان: Microsoft/Google

2. **برای وظایف حیاتی:**
   - اولویت اول: **Claude-3** (دقیق‌ترین)
   - پشتیبان: GPT-4o

3. **برای سرعت:**
   - Gemini Pro (سریع و مقرون‌به‌صرفه)

4. **برای تست:**
   - Local Models (رایگان و خصوصی)

### 🔒 امنیت

1. ✅ **هرگز `.env` را commit نکنید** (در `.gitignore` است)
2. ✅ **کلیدها را ماهانه تغییر دهید**
3. ✅ **از IAM Roles در production استفاده کنید** (AWS)
4. ✅ **کمترین دسترسی لازم را بدهید**

---

## 🐛 رفع مشکلات رایج

### مشکل: "Translation unavailable"

**علت‌های محتمل:**

- فایل `.env` وجود ندارد
- کلید API اشتباه است
- سرویس در `connectors_config.json` غیرفعال است

**راه‌حل:**

1. بررسی وجود فایل `.env`
2. تست کلید در کنسول سرویس مربوطه
3. بررسی `"enabled": true` در config

---

### مشکل: "All chat providers failed"

**علت:**

- هیچ سرویس chat فعال نیست
- همه کلیدها نامعتبرند

**راه‌حل:**

1. حداقل یک سرویس (OpenAI یا Anthropic) را فعال کنید
2. فرمت کلید را بررسی کنید (برخی پیشوند دارند)
3. اتصال اینترنت را چک کنید

---

### مشکل: Local models fail to load

**علت:**

- کتابخانه `transformers` نصب نیست
- فضای دیسک کافی نیست (~2GB)

**راه‌حل:**

```bash
pip install transformers torch
# اولین استفاده مدل را دانلود می‌کند
```

---

### مشکل: Rate limiting errors

**علت:**

- از محدودیت رایگان عبور کرده‌اید

**راه‌حل:**

1. به پلن پولی ارتقا دهید
2. سرویس‌های پشتیبان بیشتر فعال کنید
3. فاصله بین درخواست‌ها را افزایش دهید

---

## 📈 آمار و اطلاعات

### هزینه‌ها (تقریبی)

| سرویس | پلن رایگان | قیمت پولی |
|-------|-----------|----------|
| DeepL | ۵۰۰K کاراکتر/ماه | از €۴.۹۹/ماه |
| OpenAI | - | $۰.۰۳/۱K token (GPT-4o) |
| Anthropic | - | $۱۵/۱M token (Claude-3) |
| Google Gemini | ۶۰ req/min رایگان | Pay as you go |
| LibreTranslate | نامحدود (عمومی) | رایگان (خودمیزبانی) |
| Argos | نامحدود | رایگان (آفلاین) |

---

## 🎉 مراحل نهایی راه‌اندازی

### ✅ چک‌لیست نهایی

- [ ] نصب packages: `pip install requests beautifulsoup4 python-dotenv`
- [ ] ایجاد `.env` از `.env.example`
- [ ] دریافت حداقل ۲-۳ کلید API
- [ ] فعال‌سازی سرویس‌ها در `connectors_config.json`
- [ ] اجرای `test_connectors.py`
- [ ] تست با KURDO-AI Brain

### 🚀 اجرای نهایی

```bash
# تست سریع
python test_connectors.py

# اجرای کامل
python -m cad3d.cli --help
```

---

## 📞 پشتیبانی

برای مشکلات یا سوالات:

1. این راهنما را به دقت مطالعه کنید
2. مستندات سرویس مربوطه را بررسی کنید
3. سرویس‌ها را جداگانه تست کنید
4. لاگ‌های verbose را فعال کنید

---

## 🌟 نتیجه‌گیری

**KURDO-AI اکنون به پیشرفته‌ترین پلتفرم‌های هوش مصنوعی جهان متصل است!**

با این سیستم شما می‌توانید:

- ✅ ترجمه با بالاترین کیفیت (DeepL)
- ✅ گفتگو با قدرتمندترین مدل‌ها (Claude, GPT-4o)
- ✅ تحقیق آنلاین (Google Search)
- ✅ عملکرد آفلاین کامل (Local Models)
- ✅ سقوط خودکار در صورت خرابی هر سرویس

**سیستم KURDO-AI از حالت demo به یک AI واقعی و حرفه‌ای تبدیل شد! 🚀🤖**

---

*آخرین بروزرسانی: ۲۰۲۴*
*نسخه: ۲.۰ (Multi-Provider Cascade)*
